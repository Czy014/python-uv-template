# python-uv-template

This is a Python project template for personal use that uses UV management in vscode.

## quick start
```python
# install dependencies
uv sync --dev
# install project as a package
uv pip install -e .
# setup pre-commit
uv run pre-commit install
```
